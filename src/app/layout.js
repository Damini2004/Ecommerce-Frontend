import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "EcommerceHub - The Future of E-commerce Platforms",
  description: "Powerful, secure, and intuitive e-commerce platform designed for modern businesses. Multi-role dashboards, advanced analytics, and seamless user experience.",
  keywords: ["ecommerce", "platform", "dashboard", "business", "analytics"],
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}